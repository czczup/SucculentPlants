<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.succulent.wztxy.succulentplants">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:networkSecurityConfig="@xml/network_security_config"
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mine.activity.avatar.AvatarActivity" />
        <activity android:name=".mine.activity.avatar.ChangeAvatarActivity" />
        <activity android:name=".mine.activity.avatar.ChangeGenderActivity" />
        <activity android:name=".mine.activity.avatar.ChangeIntroductionActivity" />
        <activity android:name=".mine.activity.avatar.ChangeNicknameActivity" />
        <activity android:name=".mine.activity.history.RecognitionHistoryActivity" />
        <activity android:name=".mine.activity.contribution.MyContributionActivity" />
        <activity
            android:name=".mine.activity.roast.RoastActivity"
            android:label="@string/title_activity_roast"
            android:theme="@style/AppTheme" />
        <activity android:name=".blossom.activity.ArticleActivity" />
        <activity android:name=".blossom.activity.CategroyArticleActivity" />
        <activity android:name=".handbook.activity.InformationActivity"></activity>
    </application>

</manifest>